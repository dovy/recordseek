'use strict';

/**
 * @ngdoc function
 * @name recordseekApp.controller:LoadingCtrl
 * @description
 * # LoadingCtrl
 * Controller of the recordseekApp
 */
angular.module( 'recordseekApp' )
    .controller(
    'LoadingCtrl', function( $rootScope ) {
        $rootScope.service = '';
    }
);
