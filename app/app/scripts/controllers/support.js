'use strict';

/**
 * @ngdoc function
 * @name recordseekApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the recordseekApp
 */
angular.module( 'recordseekApp' )
    .controller(
    'SupportCtrl', function( $rootScope ) {
        $rootScope.service = '';
    }
);
